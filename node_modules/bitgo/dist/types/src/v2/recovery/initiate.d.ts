/**
 * @prettier
 */
import * as bip32 from 'bip32';
import * as stellar from 'stellar-sdk';
import { BitGo } from '../../bitgo';
import { BaseCoin } from '../baseCoin';
import * as config from '../../config';
interface ValidateKeyOptions {
    key: string;
    source: string;
    passphrase?: string;
    isUnsignedSweep: boolean;
    isKrsRecovery: boolean;
}
export interface InitiateRecoveryOptions {
    userKey: string;
    backupKey: string;
    bitgoKey?: string;
    recoveryDestination: string;
    walletPassphrase?: string;
}
declare type GetKrsProviderOptions = {
    checkCoinFamilySupport?: boolean;
};
/**
 * @param coin
 * @param krsProviderName
 * @param checkCoinFamilySupport - assert that krsProvider explicitly supports coin
 * @return KrsProvider
 */
export declare function getKrsProvider(coin: BaseCoin, krsProviderName: string | undefined, { checkCoinFamilySupport }?: GetKrsProviderOptions): config.KrsProvider;
/**
 * Wrapper for {@see getKrsProvider} returning void
 */
export declare function checkKrsProvider(coin: BaseCoin, krsProviderName: string | undefined, options?: GetKrsProviderOptions): void;
export declare function getIsKrsRecovery({ backupKey, userKey }: {
    backupKey: string;
    userKey: string;
}): boolean;
export declare function getIsUnsignedSweep({ backupKey, userKey }: {
    backupKey: string;
    userKey: string;
}): boolean;
export declare function validateKey(bitgo: BitGo, { key, source, passphrase, isUnsignedSweep, isKrsRecovery }: ValidateKeyOptions): bip32.BIP32Interface;
export declare function getBip32Keys(bitgo: BitGo, params: InitiateRecoveryOptions, { requireBitGoXpub }: {
    requireBitGoXpub: boolean;
}): bip32.BIP32Interface[];
export declare function getStellarKeys(bitgo: BitGo, params: InitiateRecoveryOptions): stellar.Keypair[];
export {};
//# sourceMappingURL=initiate.d.ts.map