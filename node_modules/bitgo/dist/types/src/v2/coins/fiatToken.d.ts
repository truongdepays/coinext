/**
 * @prettier
 */
import { BitGo } from '../../bitgo';
import { Fiat } from './fiat';
import { CoinConstructor } from '../coinFactory';
export interface FiatTokenConfig {
    name: string;
    type: string;
    coin: string;
    network: string;
    decimalPlaces: number;
}
export declare class FiatToken extends Fiat {
    readonly tokenConfig: FiatTokenConfig;
    constructor(bitgo: BitGo, tokenConfig: FiatTokenConfig);
    static createTokenConstructor(config: FiatTokenConfig): CoinConstructor;
    get type(): string;
    get name(): string;
    get coin(): string;
    get network(): string;
    get decimalPlaces(): number;
    getChain(): string;
    getBaseChain(): string;
    getFullName(): string;
    getBaseFactor(): number;
    /**
     * Flag for sending value of 0
     * @returns {boolean} True if okay to send 0 value, false otherwise
     */
    valuelessTransferAllowed(): boolean;
    /**
     * Flag for sending data along with transactions
     * @returns {boolean} True if okay to send tx data (CELO), false otherwise
     */
    transactionDataAllowed(): boolean;
}
//# sourceMappingURL=fiatToken.d.ts.map