/**
 * @prettier
 */
import { Trade } from './trade';
import { Affirmation } from './affirmation';
export declare enum SettlementStatus {
    CANCELED = "canceled",
    PENDING = "pending",
    REJECTED = "rejected",
    SETTLED = "settled",
    FAILED = "failed"
}
export declare enum SettlementType {
    DIRECT = "direct",
    AGENCY = "agency"
}
export declare class Settlement {
    private readonly bitgo;
    private readonly enterpriseId;
    id: string;
    requesterAccountId: string;
    status: SettlementStatus;
    type: SettlementType;
    affirmations: Affirmation[];
    createdAt: Date;
    expireAt: Date;
    settledAt: Date;
    trades: Trade[];
    constructor(settlementData: any, bitgo: any, enterpriseId: string);
}
//# sourceMappingURL=settlement.d.ts.map