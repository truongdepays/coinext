import { DecodedSignedTx, DecodedSigningPayload, DecodedUnsignedTx } from '../../../types';
/**
 * Information common to all decoded balances transfers used for testing.
 *
 * @param decoded info from a decoded tx
 */
export declare function itDecodesBalancesTransferCommon(decoded: DecodedSignedTx | DecodedSigningPayload | DecodedUnsignedTx): void;
