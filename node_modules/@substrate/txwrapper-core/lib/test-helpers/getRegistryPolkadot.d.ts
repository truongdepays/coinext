import { TypeRegistry } from '@polkadot/types';
/**
 * Get the Polkadot type registry for a given spec version for testing purposes
 *
 * @param specVersion
 * @param metadataRpc
 */
export declare function getRegistryPolkadot(specVersion: number, metadataRpc: `0x${string}`): TypeRegistry;
