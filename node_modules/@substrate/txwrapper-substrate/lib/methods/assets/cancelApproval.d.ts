import { Args, BaseTxInfo, OptionsWithMeta, UnsignedTransaction } from '@substrate/txwrapper-core';
export interface AssetsCancelApprovalArgs extends Args {
    /**
     * The identifier of the asset.
     */
    id: number;
    /**
     * The account delegated permission to transfer asset.
     */
    delegate: string;
}
/**
 * Cancel all of some asset approved for delegated transfer by a third-party account.
 *
 * @param args - Arguments specific to this method.
 * @param info - Information required to construct the transaction.
 * @param options - Registry and metadata used for constructing the method.
 */
export declare function cancelApproval(args: AssetsCancelApprovalArgs, info: BaseTxInfo, options: OptionsWithMeta): UnsignedTransaction;
